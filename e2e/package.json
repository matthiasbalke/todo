{
  "name": "todo-e2e",
  "private": true,
  "scripts": {
    "auth:admin": "bun scripts/create-admin-storage-state.ts",
    "certs:mailpit": "zsh scripts/generate-mailpit-cert.sh",
    "test": "bunx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  },
  "engines": {
    "npm": "^24.13.0"
  }
}
